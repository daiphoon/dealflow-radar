
# AGENTS.md

## 1. 适用范围与规则优先级

本文件适用于整个代码仓库。任何编码代理在规划、读取、修改、执行命令或创建文件前，必须先阅读本文件。

规则优先级：

1. 用户在当前任务中的明确要求；
2. 本文件；
3. `docs/DECISIONS/` 中已确认的架构决策记录（ADR）；
4. 其他项目文档；
5. 现有代码约定。

如规则发生实质冲突，不得自行折中。应停止相关修改，指出冲突及其影响。

第一阶段不创建嵌套的 `AGENTS.md`。未来只有目录之间确实存在不同开发规则时才可增加；目录级规则不得削弱本文件中的成本、证据、权限、安全和外部调用约束。

---

## 2. 语言规范

1. 产品文档、架构文档、数据库说明、测试说明、实施记录、界面文案和项目汇报默认使用简体中文。
2. 类名、函数名、变量名、数据库表名、字段名、API 路径、环境变量、枚举值、文件路径和命令使用英文。
3. 技术名词首次出现时可以使用“中文名称（English Term）”，之后使用中文或通用英文名称。
4. 不强行翻译成熟技术名称。
5. 除非用户明确要求，不生成中英双语文档，避免重复内容和额外 Token 消耗。
6. 代码注释只解释必要的业务规则、边界条件和非显而易见的实现。

---

## 3. 项目使命

构建一个面向未上市被投企业股东、基金投资人和投后管理人员的私有投后信息监测平台。

系统目标：

1. 维护公司、基金、投资关系、经营指标、事件和证据等结构化数据；
2. 采用按需更新、低频巡检和重大事件追踪相结合的更新机制；
3. 将公开信息或经授权的内部材料转换成可查询、可追溯的结构化事实；
4. 用户查询时直接读取数据库，不在同步请求链路中临时调用大模型生成完整答案；
5. 识别融资、重大合同、经营变化、治理变化、司法合规风险、产品技术进展和退出进程；
6. 使用固定模板低成本生成公司即时信息、投资组合周报和风险提示；
7. 为未来接入 Kimi、DeepSeek、官方公开数据源和授权商业数据库预留标准接口；
8. 对搜索、模型、商业数据库、存储和基础设施成本进行记录、限制和审计。

产品定位：

> 后台按需更新和低频巡检，前台实时读取数据库，重大事件可追踪、可复核的未上市企业投后监测平台。

“实时查询”是指用户即时读取已有数据库结果，不代表系统持续扫描全网。

---

## 4. 当前项目阶段

当前阶段：

`DEMO / VALIDATION`

默认条件：

- 前期用户很少，查询频率较低；
- 优先验证产品价值、数据准确性、事件命中率和单家公司成本；
- 公开 Demo 使用 10—20 家虚构公司；
- 真实验证只选择 5—10 家公司，并且必须在私有、受控环境中进行；
- 默认不对全部投资组合进行自动巡检；
- 暂无生产级 SLA；
- 除已有订阅和已有 API 外，尽量不新增付费服务；
- 开发环境是项目负责人的 Mac；
- Demo 依据 ADR-0014 部署在腾讯云中国香港云服务器；该环境只服务邀请制 MVP，不等于中国大陆备案或正式商业上线；
- 不部署到项目负责人的家庭 Ubuntu 服务器；
- 外部测试用户不依赖 Tailscale；服务器管理入口可以使用 Tailscale，但不得把它作为客户访问方式；
- 业务代码和数据模型不绑定特定云厂商；当前部署选择腾讯云不改变 Provider 独立性和可迁移要求。

除非当前验收标准明确要求，不得提前建设大规模生产基础设施。

---

## 5. Demo 阶段非目标

除非用户明确要求，不实现：

- 分钟级或秒级全网监控；
- 每日自动扫描全部公司；
- 移动 App；
- 完整 SaaS 计费系统；
- Kubernetes；
- 多地域部署；
- 微服务拆分；
- Kafka；
- 独立向量数据库；
- 对全部历史文档的自然语言问答；
- 自动估值；
- 把财务预测或模型推断写成事实；
- 严重负面信息完全自动发布；
- 绕过登录、验证码、频率限制或反爬机制；
- 抓取受限制的商业数据库前端页面；
- 短信通知；
- 复杂视觉设计系统；
- 没有新信息时重复生成公司报告。

优先完成一个可以验证的端到端垂直闭环，而不是追求功能数量。

---

## 6. 成本优先原则

成本控制包含两部分：

1. 运行成本：搜索、模型、商业数据、存储、服务器和推送成本；
2. 开发成本：Codex 上下文、重复读取、无效规划、过度文档和过度工程。

核心成本指标：

- 每条有效事件综合成本；
- 每家活跃公司月均成本；
- 无变化任务占比；
- 缓存命中率；
- 搜索结果有效率；
- 模型抽取成功率；
- 误报和人工复核成本；
- 单次开发任务的有效修改比例。

成本目标不是简单选择最便宜的模型，而是降低每条可靠有效信息的综合成本。

---

## 7. 用户查询规则

同步查询路径必须：

1. 读取 PostgreSQL 中已有的公司快照、事件、指标和证据元数据；
2. 返回最近一次已发布结果；
3. 显示数据基准时间、最后检查时间和新鲜度状态；
4. 不同步等待外部搜索或大模型；
5. 不因为用户打开页面而重新生成完整公司报告。

数据过期时：

1. 立即返回现有数据；
2. 标记 `freshness_status = stale`；
3. 在满足预算、冷却期和去重规则时创建后台更新任务；
4. 告知用户当前展示的是最近一次结果；
5. 合并同一公司的重复更新请求。

不得把“未发现新的公开信息”表述为“公司经营正常”。

---

## 8. 默认更新策略

所有更新频率必须配置化，不得硬编码在业务逻辑中。

Demo 默认规则：

- 未被查询且未被关注的普通公司：不自动更新，或每 30 天低频检查；
- 最近被查询的公司：缓存有效期默认 14 天；
- 用户主动关注的公司：默认每 7 天检查；
- 最近 30 天出现实质事件的公司：临时提高到每 2—3 天检查；
- 高风险公司：必要时每日检查，风险稳定后自动降频；
- 工商主体及基础身份信息：默认 30 天；
- 普通新闻和业务动态：默认 14 天；
- 融资、重大合同、司法风险和退出进程：关注公司默认 7 天；
- 用户手动申请更新：受冷却期、预算和任务去重约束。

每家公司至少维护：

- `last_identity_checked_at`
- `last_structured_data_checked_at`
- `last_news_checked_at`
- `last_material_event_at`
- `next_check_at`
- `refresh_priority`
- `refresh_reason`
- `freshness_status`
- `consecutive_no_change_runs`
- `cooldown_until`

长期无变化的公司自动降频；近期发生重大变化的公司可以临时升频。

---

## 9. 外部调用总开关

开发和自动测试默认配置：

```env
APP_MODE=demo
EXTERNAL_CALLS_ENABLED=false
PAID_API_CALLS_ENABLED=false
AUTO_REFRESH_ENABLED=false
```

任何付费或可能计费的操作前必须检查：

- 单次任务预算；
- 单家公司日预算和周预算；
- 租户周预算和月预算；
- 系统周预算和月预算；
- 搜索调用次数上限；
- 输入 Token 上限；
- 输出 Token 上限；
- 同一公司冷却期；
- 相同查询缓存；
- 文档内容哈希；
- 外部 Provider 是否已启用。

超出预算时：

1. 不继续调用；
2. 保留数据库查询功能；
3. 将任务标记为 `budget_deferred`；
4. 记录预计费用和阻塞原因；
5. 不自动切换到更昂贵的模型或数据源；
6. 可以降级为只使用已有数据或低成本结构化来源。

必须支持 `dry-run`，展示：

- 准备更新的公司；
- 准备调用的 Provider；
- 预计搜索次数；
- 预计输入、输出 Token；
- 预计费用；
- 但不实际执行外部调用。

---

## 10. 模型使用策略

当前已有或计划使用的模型资源包括 DeepSeek-V4-Pro API。具体模型名称、供应商、参数和价格必须通过配置管理，不得写死在业务逻辑中。

大模型默认只用于：

- 公司实体匹配辅助；
- 文档相关性分类；
- 结构化事件抽取；
- 事件分类；
- 重复事件辅助判断；
- 有明确证据约束的短事实摘要；
- 用户明确授权后的深度分析。

默认原则：

- 优先低成本或非推理模式；
- 使用低温度；
- 强制结构化 JSON；
- 限制输出长度；
- 只传入必要的公司身份信息、标题和证据片段；
- 失败最多自动重试一次；
- 记录 `provider`、`model`、`prompt_version`、参数、Token、延迟和估算费用；
- JSON 解析或校验失败时不得写入已发布事件。

不得调用模型：

- 因用户打开页面而生成完整回答；
- 没有新文档或新结构化记录时重新总结；
- 对内容哈希未变化的文档重复处理；
- 无依据估算营收、利润、现金、估值、持股比例或合同金额；
- 将不确定推断写入事实字段；
- 自动断言公司“经营正常”“经营恶化”或“即将上市”。

未知信息必须保持未知。

---

## 11. Kimi 集成边界

项目未来可能订阅 Kimi Allegretto，并利用 Kimi 产品端的 Agent 和专业数据库能力进行研究。

必须严格区分：

- Kimi 消费端会员和 Agent；
- Kimi Work 或 Kimi Claw；
- Kimi Code；
- Kimi 开放平台 API；
- 获得正式授权的商业数据库 API。

不得假设：

- Allegretto 会员额度可以直接转化为网站后台 API 额度；
- 网站可以通过未公开接口直接调用天眼查、同花顺 iFind 或其他专业数据库；
- 会员端查询结果可以无限缓存、再分发或面向第三方商业展示；
- Kimi 搜索结果可以替代正式工商、司法或监管事实来源。

系统应预留：

- `MockSearchProvider`
- `ManualResearchImportProvider`
- `KimiAgentImportProvider`
- `KimiScheduledResearchImportProvider`
- `KimiOpenPlatformProvider`
- `LicensedBusinessDataProvider`
- `OfficialPublicDataProvider`

允许的未来接入方式：

1. 人工从 Kimi Agent 导出结构化结果后导入；
2. Kimi Work、Kimi Claw 或其他官方能力输出 JSON、CSV、Excel 或 Markdown 后导入；
3. 使用 Kimi 开放平台的正式 API；
4. 使用单独获得授权的专业商业数据 API。

禁止：

- 调用未公开的消费端接口；
- 抓取 Kimi 账户页面；
- 自动保存登录凭据或会话 Cookie；
- 绕过频率限制；
- 未经授权再分发商业数据库内容。

Demo 必须在没有 Kimi 的情况下完整运行。

---

## 12. Provider 独立性

外部能力必须通过适配器接口接入，至少包括：

- `SearchProvider`
- `StructuredDataProvider`
- `ResearchImportProvider`
- `DocumentFetcher`
- `LLMProvider`
- `StorageProvider`
- `NotificationProvider`
- `JobQueueProvider`

业务逻辑不得直接依赖具体供应商 SDK。

Provider 选择必须通过配置和依赖注入完成。测试和 Demo 必须提供 Mock 实现。

第一阶段优先采用：

- PostgreSQL；
- 数据库任务表；
- 单独 Worker；
- Cron；
- 本地文件或持久化卷。

不得仅因为未来可能扩展就提前引入 Celery、Redis、Kafka、MinIO 或 Kubernetes。出现真实容量瓶颈后，再通过 ADR 决定升级。

---

## 13. 数据源原则

数据源优先级：

1. 政府、法院、监管机构及正式公开系统；
2. 公司官网、官方账号和正式公告；
3. 经许可的商业数据库或 API；
4. 主流财经媒体和行业媒体；
5. Kimi 等联网研究工具；
6. 自媒体和社交平台只作为线索来源。

不得：

- 绕过验证码、登录、付费墙或反爬措施；
- 违反服务条款批量抓取；
- 未经授权复制和长期保存大量受版权保护全文；
- 将低质量来源单独作为重大负面结论的依据。

普通新闻原则上只保存：

- URL；
- 标题；
- 发布时间；
- 来源；
- 抓取时间；
- 内容哈希；
- 支撑结论的最小必要证据片段；
- 在许可范围内的快照或存储引用。

关键官方证据可以长期保存快照。保存期限和授权范围必须可配置。

---

## 14. 证据和数据血缘

原始证据优先于生成文本。

必须分别保存：

- 原始文档及元数据；
- 公司提及和实体匹配；
- 结构化事件；
- 事件证据；
- 指标观测；
- 公司快照；
- 生成报告；
- 模型调用记录；
- 人工审核记录。

每个已发布事件必须关联至少一条证据。

不得使用 AI 摘要替代原始证据。原始文档、指标观测和历史事件原则上追加保存，不直接覆盖。

公司快照、状态摘要和报告属于派生数据，必须可以从历史事件和指标重新生成。

必须区分：

- `occurred_at`：事件实际发生时间；
- `published_at`：来源发布时间；
- `observed_at`：系统发现时间；
- `as_of_date`：指标或报告基准日；
- `created_at`：数据库记录创建时间。

---

## 15. 公司身份解析

事件发布前必须完成公司身份解析。

身份依据优先级：

1. 统一社会信用代码；
2. 工商全称；
3. 曾用工商名称；
4. 注册地和法定代表人；
5. 官方网站或官方账号；
6. 经验证的品牌、产品、集团和子公司关系。

简称或品牌名不能单独支撑严重负面事件。

身份存在歧义时：

- 不得发布；
- 标记为 `unresolved`；
- 进入人工审核队列；
- 保留候选证据；
- 不合并到公司快照。

---

## 16. 事件分类

至少支持：

- `financial_operation`
- `financing_cap_table`
- `contract_commercial`
- `product_technology`
- `governance_people`
- `legal_compliance`
- `capacity_assets`
- `exit_liquidity`
- `information_quality`

主要内容：

### `financial_operation`

- 营收、利润、毛利率；
- 订单、出货量和销量；
- 现金储备和现金消耗；
- 欠薪、裁员、停工和停产；
- 客户、供应商变化；
- 产能和产能利用率；
- 行业特有经营指标。

### `financing_cap_table`

- 新融资、投资方、金额和估值；
- 注册资本变化；
- 新增或退出股东；
- 控制权变化；
- 股权质押、冻结和司法拍卖；
- 老股转让、回购和对赌触发。

### `contract_commercial`

- 重大合同和中标；
- 大客户合作；
- 海外订单；
- 进入关键供应链；
- 首单、首个量产或首个商业化项目；
- 合同取消、客户流失和重大违约。

### `product_technology`

- 新产品；
- 认证、注册证和生产许可；
- 临床试验、药品和器械审批；
- 专利、知识产权和技术里程碑；
- 技术路线变化；
- 产品召回和质量问题；
- 研发暂停。

### `governance_people`

- 法定代表人、董事、监事和高管变化；
- 创始人或核心技术人员离职；
- 组织重组；
- 大规模裁员；
- 高管被调查、失联或限制消费。

### `legal_compliance`

- 诉讼、仲裁和执行；
- 失信和限制高消费；
- 行政处罚和经营异常；
- 税务、环保和安全生产；
- 数据安全、网络安全和知识产权纠纷；
- 破产、清算、注销和停业。

### `capacity_assets`

- 新建工厂和生产线；
- 项目开工、投产或延期；
- 核心资产抵押或查封；
- 经营场所关闭；
- 环评、能评、土地和施工许可；
- 供应链中断。

### `exit_liquidity`

- IPO 辅导；
- 申报、问询、中止和撤回；
- 新三板挂牌；
- 并购和股权转让；
- 回购、清算和完全退出。

### `information_quality`

- 最后更新时间；
- 数据源覆盖；
- 关键数据缺口；
- 来源冲突；
- 可信度；
- 审核状态。

---

## 17. 事件评价

必须分别存储：

- `direction`
- `materiality_score`
- `risk_severity`
- `confidence_score`
- `source_quality`

不得合并成一个不可解释的综合分数。

评分必须保留可解释组成因素，例如：

- 金额或规模；
- 是否影响核心业务；
- 是否涉及控制权；
- 是否影响现金流、持续经营或退出；
- 是否属于首次或里程碑事件；
- 来源权威性；
- 独立来源数量；
- 公司身份匹配程度。

重大合同不能只按营收比例判断，因为未上市公司营收可能未知。可以依据：

- 公开金额达到配置阈值；
- 交易对手属于关键客户；
- 属于首单、首个量产或首个海外项目；
- 明确影响核心产品商业化；
- 管理员人工标记。

---

## 18. 未确认线索和按需人工复核

只有公司身份、曾用名、品牌或母子公司关系无法唯一解析时，必须进入人工审核队列；身份未解析前不得生成公司事件或更新快照。

以下情况由程序自动保存为 `unconfirmed_lead`，不进入已确认事实、风险结论或公司快照，也不默认创建人工任务：

- 来源相互冲突；
- 只有低质量来源；
- 涉及欺诈、刑事、失联、破产、停业或核心人员调查；
- 可能实质影响估值、持续经营或退出；
- 模型置信度低于配置阈值；
- 金额或单位异常；
- 链接不可访问或未完成来源检查；
- 存在撤稿、纠错或后续裁判。

未确认线索只有在用户需要形成正式判断、纠错或撤回时才按需人工复核。不得把线索状态或自动展示解释为人工背书。

发布原则：

- 满足版本化自动发布策略的官方例行事实可以直接发布；
- 严重负面可以自动展示为未确认线索，但不得自动升级为已确认事实；
- 非官方重大负面原则上需要至少两个独立来源；
- 指控不得改写为已经认定的事实；
- 支持 `candidate`、`in_review`、`published`、`rejected`、`retracted`、`corrected`；
- 自动路由必须记录策略版本和原因；人工决定必须记录审核人、时间和理由。

---

## 19. 财务数据规则

财务指标必须标记来源性质：

- `audited`
- `regulatory_disclosure`
- `company_reported`
- `manager_reported`
- `media_cited`
- `third_party_estimate`
- `unknown`

禁止将第三方估算或模型推断写入审计数据或公司披露字段。

历史指标不得被单一最新值覆盖。每次观测保存：

- 指标定义；
- 数值；
- 单位；
- 统计期间；
- 基准日；
- 来源；
- 可信度；
- 是否经人工复核。

无法获得公开营收、利润或现金流时，明确显示：

> 暂无可靠公开数据。

---

## 20. 私密数据和权限

公开监测信息与内部投资数据必须逻辑隔离。

敏感数据包括：

- 投资金额；
- 持股比例；
- 内部估值；
- MOIC、TVPI；
- 投资协议、对赌和回购条款；
- 未公开财务报表；
- 投委会材料；
- 管理人季度报告；
- 董事会和经营材料。

必须实现或预留：

- 租户隔离；
- 基金级授权；
- 基于角色的访问控制；
- 敏感记录的 `visibility_scope`；
- 敏感数据读写审计；
- 公开 Demo 不使用真实敏感数据；
- 真实报告、私密文件和密钥不进入 Git；
- 私有导入目录加入 `.gitignore`；
- 日志不输出完整私密文档、API Key、密码、Cookie 或不必要的个人信息。

同一家公司可以被多个基金投资。公开事件可以复用，投资金额、持股比例和内部估值必须按基金隔离。

---

## 21. 数据库和迁移

主数据库使用 PostgreSQL。

所有数据库结构变化必须使用 Alembic 或项目最终确定的迁移工具。

不得修改已经应用的历史迁移；修正时创建新迁移。

核心表应包含适当的：

- 主键；
- 外键；
- 唯一约束；
- 索引；
- 时间字段；
- 租户或可见范围字段；
- 状态字段；
- 数据来源和血缘字段。

不得只保存一段生成式公司摘要。历史指标和事件不能被一个“最新值”覆盖。

采集、抽取和发布必须具备幂等性。相同 URL、内容哈希、来源记录或事件指纹重复运行时，不得产生重复事件。

事件发布和快照更新应使用事务。

---

## 22. MVP 核心数据模型

第一阶段设计完整领域模型，但后续开发只优先实现端到端闭环需要的核心表：

- `tenants`
- `users`
- `roles`
- `funds`
- `companies`
- `company_aliases`
- `investments`
- `sources`
- `raw_documents`
- `entity_mentions`
- `events`
- `event_evidence`
- `metric_definitions`
- `metric_observations`
- `company_snapshots`
- `refresh_policies`
- `refresh_jobs`
- `refresh_runs`
- `research_imports`
- `review_queue`
- `usage_ledger`
- `prompt_versions`
- `audit_logs`

未来能力可以先进入设计文档，不得为了尚未验证的需求提前实现大量表和接口。

---

## 23. 报告生成

普通公司报告和组合报告应主要由以下结构化数据通过固定模板生成：

- 公司快照；
- 已发布事件；
- 指标观测；
- 证据元数据；
- 数据新鲜度；
- 信息缺口。

不得在每次访问时调用大模型重写完整历史。

大模型生成的分析评论必须：

- 可选；
- 与事实层分开；
- 有明确标签；
- 关联具体事件和证据；
- 缓存并版本化；
- 不进入默认同步查询链路；
- 未经人工确认不得作为投资建议。

Demo 优先支持 HTML 或 Markdown 报告，PDF 放在核心闭环稳定之后。

---

## 24. 前端呈现

优先页面：

1. 公司列表；
2. 公司详情；
3. 自上次查看以来发生的变化；
4. 重大风险中心；
5. 事件证据和时间线；
6. 人工审核队列；
7. 数据新鲜度和来源覆盖；
8. 成本仪表盘；
9. 简单组合周报。

每条事件至少展示：

- 事件日期；
- 分类；
- 方向；
- 重要性；
- 风险级别；
- 摘要；
- 来源；
- 证据；
- 可信度；
- 审核状态；
- 系统发现时间。

不得只展示一个无法解释的综合评分。

---

## 25. 技术与部署原则

建议的 Demo 技术栈：

- 后端：Python、FastAPI、Pydantic、SQLAlchemy、Alembic；
- 数据库：PostgreSQL；
- 前端：Next.js、TypeScript；
- 后台任务：数据库任务表、单独 Worker 和 Cron；
- 文件存储：通过 `StorageProvider` 抽象，Demo 使用本地持久化卷；
- 部署：Docker Compose；
- 测试：Pytest，必要时使用 Playwright；
- 日志：结构化日志；
- 密钥：环境变量或部署平台 Secret。

不得因为“未来可能扩展”就提前引入复杂基础设施。出现真实瓶颈后，通过 ADR 评估 Redis、Celery、对象存储或其他组件。

---

## 26. 开发工作方式

开始任务前：

1. 阅读本文件；
2. 只阅读与当前任务直接相关的文档和文件；
3. 不对全仓库进行无差别扫描；
4. 确认当前阶段和验收标准；
5. 使用简短计划说明准备修改什么；
6. 只在答案会阻塞安全实现时提问。

修改过程中：

- 使用小步、可审查的修改；
- 不做无关重构；
- 不无理由升级依赖；
- 不引入没有验收依据的基础设施；
- 优先复用现有模式；
- 行为变化必须增加或更新测试；
- 不修改无关文件；
- 不因追求形式完整而扩张任务范围。

修改后：

1. 先运行针对性测试；
2. 必要时再运行更广泛测试；
3. 说明实际执行的命令；
4. 如实报告失败；
5. 只更新受影响文档；
6. 更新简洁实施记录；
7. 达到当前验收标准后停止。

---

## 27. 开发 Token 节约

编码代理必须：

- 保持计划简洁；
- 不重复转述完整需求；
- 引用现有文档，不复制大段内容；
- 只读取当前任务相关文件；
- 避免反复读取大型文件；
- 优先定点修改，不整文件重写；
- 不输出冗长思考过程；
- 不在决策已经记录后反复罗列替代方案；
- 不为非阻塞问题反复提问；
- 一次只完成一个明确阶段；
- 验收条件满足后停止；
- 不自动进入下一阶段；
- 不生成没有实际用途的模板和占位文件。

重大架构决策写入 `docs/DECISIONS/`，避免反复讨论。

---

## 28. 测试规则

自动测试不得依赖付费外部服务。

至少覆盖：

- 确定性业务规则；
- Provider Mock 合约；
- 采集幂等性；
- 公司身份解析；
- 文档和事件去重；
- 预算闸门；
- 更新冷却和任务合并；
- 权限隔离；
- 事件审核状态；
- 数据迁移；
- 最小端到端 Demo 闭环。

外部集成测试必须：

- 显式启用；
- 单独标记；
- 默认在 CI 中跳过；
- 运行前显示预计调用和成本；
- 不自动使用真实付费密钥。

---

## 29. 文档

项目文档原则上包括：

- `README.md`
- `docs/00-product-requirements.md`
- `docs/01-domain-model.md`
- `docs/02-system-architecture.md`
- `docs/03-database-design.md`
- `docs/04-event-taxonomy.md`
- `docs/05-data-source-strategy.md`
- `docs/06-cost-control.md`
- `docs/07-security-compliance.md`
- `docs/08-api-design.md`
- `docs/09-ui-wireframes.md`
- `docs/10-implementation-plan.md`
- `docs/11-test-strategy.md`
- `docs/12-operations-runbook.md`
- `docs/DECISIONS/`
- `docs/IMPLEMENTATION_LOG.md`

文档必须简洁、可执行，避免重复。

`IMPLEMENTATION_LOG.md` 每条只记录：

- 日期；
- 任务；
- 关键文件；
- 实际命令；
- 测试结果；
- 未解决阻塞。

不得记录对话或完整思考过程。

---

## 30. 变更控制

不得静默改变：

- 更新频率；
- 事件分类；
- 评分语义；
- 数据权限边界；
- Provider 接口；
- 成本上限；
- 重大负面发布规则；
- 财务事实口径；
- Kimi 接入边界。

重大决策写入：

`docs/DECISIONS/ADR-xxxx-*.md`

---

## 31. 完成定义

任务只有在以下条件全部满足时才算完成：

- 当前要求的行为或文档已完成；
- 相关验证或测试通过；
- 没有意外产生付费外部调用；
- 数据血缘和证据关系完整；
- 权限边界没有被破坏；
- 成本影响已经说明；
- 相关文档已经同步；
- 已知限制得到如实报告；
- 没有擅自进入下一阶段。

不得把未经测试或验证的行为声明为已完成。
