# 10 分阶段实施计划

> 本文件是项目唯一的阶段推进看板。里程碑以验收闸门推进，不设日期或工期承诺；下方历史记录只用于审计。

## 1. 看板使用规则

| 状态 | 含义 |
| --- | --- |
| `已完成` | 验收条件已有证据，相关 PR 已合并或运营报告已确认 |
| `进行中` | 当前唯一主要工程或运营里程碑 |
| `等待决策` | 涉及方向、权限、许可、成本、认证、部署或上线的重大选择，须由项目负责人确认 |
| `待开始` | 前置闸门尚未通过，不得提前扩张范围 |
| `阻塞` | 缺少授权、数据、账户或存在不能安全绕过的问题 |
| `可选后置` | 不影响当前上线主路径，只有真实需求证据充分时才立项 |

1. 同一时间只保留一个主要里程碑；非阻塞优化记录后置，不顺手开发。
2. 数据权限、供应商授权、自动发布、目标用户、认证、部署、价格和支付等重大选择必须先给出建议与利弊，等待项目负责人决定。
3. 普通实现细节、测试方法和不改变产品边界的界面调整可在当前范围内自主决定。
4. 每个里程碑只有在验收证据、已知限制、回滚方式和下一闸门均已记录后才可关闭。

## 2. 当前项目位置

```mermaid
flowchart LR
    F0["F0 核心验证底座<br/>已完成"] --> M0["M0 受控来源监测<br/>已完成"]
    M0 --> M1["M1 数据价值与数据源准入<br/>已完成"]
    M1 --> M2["M2 可重复真实内容生产 V1<br/>进行中"]
    M2 --> M3["M3 正式认证与账户安全<br/>待开始"]
    M3 --> M4["M4 个人用户核心闭环<br/>待开始"]
    M4 --> M5["M5 生产部署与运营安全<br/>待开始"]
    M5 --> M6["M6 邀请制真实用户测试<br/>待开始"]
    M6 --> M7["M7 个人订阅公开上线<br/>待开始"]
    M7 -."需求证据充分".-> M8["M8 机构自助管理<br/>可选后置"]
```

| 编号 | 里程碑 | 状态 | 当前证据 / 前置 | 建议交付单位 |
| --- | --- | --- | --- | --- |
| F0 | 核心验证底座 | `已完成` | PR #9—#11；双通道、RLS、身份消歧、研究导入和共享事实晋升/拒绝/撤回已验证 | 已完成 |
| M0 | 受控官方来源监测与候选队列 | `已完成` | PR #12 已合并；来源登记、SSRF 防护、Worker、候选队列及真实小样本通过 | 已完成 |
| M1 | 数据价值与数据源双重准入 | `已完成` | 26 条真实候选已处理，2 条完成转化；天眼查授权准入并由 PR #13 实现；PR #14 修正全局工商曾用名索引 | 运营报告 + 已合并代码 PR |
| M2 | 可重复真实内容生产 V1 | `进行中` | 候选直接交接、低频到期调度、许可闸门及本地验收已完成；等待代码 PR 审查与合并 | 1 个可回滚代码 PR |
| M3 | 正式认证与账户安全 | `待开始` | M2 通过后先决定首批用户和认证方案 | 决策文档 + 最多 2 个 PR |
| M4 | 个人用户核心使用闭环 | `待开始` | 需服务端可信用户身份 | 最多 2 个 PR |
| M5 | 生产部署与运营安全 | `待开始` | 核心用户闭环稳定；先决定部署环境 | 1 个基础设施 PR + 验收记录 |
| M6 | 邀请制真实用户测试 | `待开始` | M0—M5 全部通过，数据许可和安全边界明确 | 运营验收报告 |
| M7 | 个人订阅公开上线 | `待开始` | 邀请测试达到事先确认的价值、安全和运营门槛 | 权益、开通/支付、上线加固 2—3 个 PR |
| M8 | 机构自助管理 | `可选后置` | 有明确机构付费或规模化管理需求后再立项 | 独立里程碑 |

当前仍处于 `DEMO / VALIDATION`。核心技术闭环已形成，但正式认证、个人留存闭环、生产部署和真实用户价值验证尚未通过，不适合公开上线。

## 3. 当前里程碑 M2：可重复真实内容生产 V1

**目标**：把一次性真实候选验证变成可重复、可审计、低频运行的内容生产流程，同时保持所有共享事实人工晋升。

工作节点：

- [x] 天眼查授权工商身份 Provider V1（PR #13），不进入同步查询和自动发布；
- [x] 公司为全局工商主体，基金与投资关系只是私有叠加（PR #14）；
- [x] 在当前工程分支中实现“值得研究候选 → 私有研究底稿/候选事件”直接交接，取消手工复制 JSON；
- [x] 在当前工程分支中实现一次性到期调度命令，dry-run 优先，并复用既有 Worker 的重试和过期租约恢复；
- [x] 区分 `public_access`、`permission_confirmed`、`unclear` 和 `restricted`；只有明确授权记录才能后续进入共享晋升；
- [x] 在隔离的真实试运行副本上完成一次候选交接、一次无变化调度 dry-run 与权限回归；
- [x] 完成 SQLite/PostgreSQL 迁移、RLS、后端、前端和浏览器本地验收；
- [x] 创建代码 PR 并保持未合并，等待项目负责人审查和 CI 结果。

验收闸门：同一批公司可重复运行；新增、无变化、失败和重复有确定结果；错误公司关联为 0；私有候选不跨作用域；单公司和单条有效事实成本可记录；手工操作相较 M1 减少；`AUTO_PUBLISH_ENABLED=false`。

明确不做：全网搜索、GSXT 抓取、LLM 自动写事实、自动共享、严重负面自动发布、通用调度平台、Celery/Redis/Kafka 或分布式爬虫。

## 4. 后续里程碑范围和决策闸门

| 里程碑 | 核心目标 | 最小范围 | 验收闸门 | 项目负责人决策 |
| --- | --- | --- | --- | --- |
| M3 | 用可信账户和服务端会话替代 `X-Demo-User-Id` | 邀请/受控注册、登录退出、会话失效/恢复、角色映射、审计和负向测试 | 不能伪造他人身份，后端强制权限，四类用户浏览器回归 | 首批用户顺序；托管认证还是自建（建议优先成熟托管方案） |
| M4 | 形成个人用户“查询→关注→看变化→回访”闭环 | watchlist、请求收录、数据新鲜度/最近变化、固定模板报告、测试用权益上限 | 无基金个人可完成闭环，服务端执行上限，无私有字段泄露 | 邀请测试的查询/关注/报告上限和收录规则 |
| M5 | 建立可恢复、可监控、可安全停止的外部试用环境 | HTTPS、Secret、环境隔离、迁移/应用回退、加密备份恢复、日志告警与熔断 | 干净环境可部署，备份可恢复，故障可发现/停止/回退 | 云服务、地域、域名、数据库托管和备份保留策略 |
| M6 | 邀请制验证用户价值、运营成本、安全和许可 | 建议 5—10 名个人、至多 1—2 家友好机构、30—50 家已核验公司；只修阻塞缺陷 | 事先指标达标，无未处置严重泄露，许可支持展示，恢复/支持流程可用 | 邀请样本、成功指标和 Go/No-Go 阈值；测试中不为通过而下调 |
| M7 | 补齐个人订阅的交易、权益、合规和支持闭环 | plan/subscription/entitlement、开通/支付、到期/取消、条款隐私、许可矩阵、纠错和客服 | 开通至到期闭环、数据展示授权、生产故障恢复和最终上线决策均通过 | 定价、收费方式、法律文本和公开上线 Go/No-Go |
| M8 | 用真实机构需求驱动自助管理 | organization、成员邀请、席位、基金/组合/公司授权、赞助访问 | 只有成交或持续试点证明现有人工流程是瓶颈时才启动 | 是否存在足以立项的机构付费需求 |

## 5. 交付与审计规则

1. 代码里程碑原则上不超过 2 个 PR；运营验证可只交付报告，不为形式创建代码。
2. 每个交付节点必须可独立测试和回滚，不产生长期不可用的半成品。
3. 每次交付必须报告完成内容、验收证据、数据/权限/费用影响、计划偏差、自查修复、未解决事项、下一闸门及 Git/数据库/服务状态。
4. 当前 M2 验收前不开始正式认证、watchlist、订阅、支付、organization 重构或生产部署。

## 6. 历史阶段规划与实施记录

以下保留早期阶段设计和已完成切片，用于审计与理解演进。旧阶段编号不再作为当前排期入口；其中日期是历史证据，不是期限或上线承诺。

第 1 阶段只完成设计。本文件提出后续最小路径，不构成自动开始授权。

## 第 2 阶段：最小数据闭环

范围：Docker PostgreSQL 与 Alembic；最小租户/基金授权；公司与别名；投资关系；来源、Mock 原始文档、实体提及、候选事件、证据、审核、公司快照、刷新任务和用量台账；一个固定模板公司页面。Provider 只用 Mock，无网络。

建议实现表：`tenants`、`users`、`roles`、`user_role_assignments`、`funds`、`fund_access_grants`、`companies`、`company_aliases`、`investments`、`sources`、`raw_documents`、`entity_mentions`、`events`、`event_evidence`、`company_snapshots`、`refresh_jobs`、`review_queue`、`usage_ledger`。其余完整模型暂留文档。

验收：10 家虚构公司可从 Mock 文档幂等入库 → 解析主体 → 形成候选 → 人工审核 → 发布事件与快照 → 页面展示证据/新鲜度/授权投资概览/成本；重复运行不产生重复文档或事件；同步查询外部调用为 0；跨基金越权测试拒绝。

不做：真实 API、真实 LLM、自动 Cron、通用报表系统、通知、PDF、生产身份系统。主要风险是首版表过多和权限测试遗漏；通过只实现上述闭环、迁移审查和负向测试控制。预计新增外部成本：¥0。

## 第 3 阶段：低成本真实数据验证

范围：只选 5—10 家私有测试公司；人工研究导入、Kimi 官方/人工导出导入、DeepSeek 严格结构化抽取、少量官方公开来源；完善身份和证据审核。

验收：每个 Provider 先 `dry-run`；导入许可可追踪；文档哈希不变时 LLM 调用为 0；抽取 Schema 成功率、误报率、身份例外量和单条有效事件成本有实测；严重负面自动升级为已确认事实数为 0。

不做：批量全组合扫描、商业数据库前端抓取、无授权再分发。风险是数据许可、主体歧义、模型幻觉和费用漂移；以白名单来源、预算上限和小样本人工复核控制。预计新增外部成本：用户批准前为 ¥0，批准后按 Provider 价格配置和任务预算封顶。

## 第 4 阶段：自动调度与周报

范围：版本化 TTL、`next_check_at`、Cron、Worker 租约、关注公司、动态升降频、任务合并、固定模板周报、成本仪表盘和可选邮件/飞书通知。

验收：过期先返回后更新；并发请求只产生一个活跃任务；租约过期可恢复；预算超限转 `budget_deferred`；长期无变化降频、近期实质事件升频；周报事实水位可复现。

不做：Celery/Redis/Kafka、分钟级全网监控。风险是队列积压、重复通知和错误升频；以幂等键、投递台账、积压指标和回退策略控制。预计新增外部成本：依实测任务量、通知方式和部署资源配置，必须先做 10/50 家影子运行。

## 第 5 阶段：商业化与扩展

范围：强化多租户、正式商业数据、顾问服务、生产认证、计费、PDF、审计导出和更高并发；只有实测需要时评估 Redis/Celery、对象存储或向量检索。

验收：租户隔离渗透/权限测试、备份恢复演练、数据许可清单、预算结算、服务目标和容量测试达标；商业功能逐项 ADR 批准。

不做：未经容量证据的大规模基础设施。风险是合规义务、供应商锁定、运营审核量和成本曲线；以合同审查、Provider 抽象、容量阈值和人工服务流程控制。预计新增外部成本：待商业模型、SLA 和供应商合同确定后单独立项。

## 第 2 阶段确认结果

2026-07-14 已确认：

1. Schema 预留多租户，运行时先使用单 Demo 租户；测试同时保留第二个租户/基金用于越权验证。
2. 只使用固定虚构测试身份验证 RBAC，不实现生产登录系统。
3. 验收界面只实现公司列表与公司详情；审核队列通过 API 和自动测试覆盖。

当前最小闭环已完成 SQLite 离线测试、PostgreSQL 16 迁移与 Schema 漂移检查、迁移/应用账户分离和非所有者 RLS 负向验证，以及 API、服务端渲染和桌面浏览器视觉验收。第 2 阶段验收项已完成；第 3 阶段已用 2 家用户授权真实公司完成首轮私有、受控的人工导入验证，但仍未启用外部 Provider。

## 按需缓存与 Mock Worker V1

2026-07-15 在不启用真实 Provider、不增加数据库表和不启动自动 Worker 的前提下，先实现第 4 阶段的一条低风险基础切片：配置化 TTL 与冷却、动态新鲜度、详情过期入队、列表不扇出、活动任务合并。随后补充单次 Mock Worker：按租户领取一个任务、维护短租约和心跳、恢复过期租约、模拟无变化检查、记录零外部调用和零费用；无快照时保持未知，不编造事实。

这两条切片只验证缓存、任务数量和队列恢复，不表示已进入完整第 4 阶段。`refresh_policies` 表、预算闸门、`next_check_at`、Cron、常驻 Worker、真实 Provider 流水线、周期心跳、重试、升降频和周报仍保持未实现。

## 人工研究导入 V1

2026-07-15 实现第 3 阶段第一条零外部成本切片：机构管理员从 Git 忽略的私有目录导入公开来源 JSON；通过文件、批次、外部记录和事件指纹保证幂等；保存批次哈希、证据和身份解析血缘；所有解析成功事件进入 `in_review`，身份未解析记录进入实体提及审核，均不自动发布或更新快照。导入批次按租户 RLS 隔离，并用 `usage_ledger` 明确记录零外部调用和零费用。

该切片不实现 API 上传、CSV/Excel/Markdown、内部敏感材料、真实身份系统、Kimi/DeepSeek 或自动调度。实体提及可以安全排队但尚不能在通用事件审核接口中完成“选择公司并重建候选”。2026-07-15 首轮真实样本的 2 条记录均通过信用代码精确命中，未出现主体歧义，因此暂不扩展身份审核流程。

## 人工审核工作台 V1

2026-07-15 补齐本机最小审核呈现：受显式安全开关保护，审核员可查看候选、事实、不确定性、三类时间、五项独立评价及证据，并用必填理由批准或驳回事件；实体提及歧义保持只读。该切片不实现正式认证、纠正/撤回、身份解析、分页筛选或公开部署，也不调用外部 Provider。

## 身份优先自动路由 V1

2026-07-15 根据 ADR-0007 将新导入从“逐事件强制审核”调整为确定性路由：只有公司身份或组织关系无法唯一解析时创建人工任务；身份已验证、证据 URL 可访问、来源 A/B、可信度达标且不属于高/极高风险的事实自动发布；其余记录保存为 `unconfirmed_lead`，不进入快照也不默认创建人工任务。每条事件保留策略版本、路由原因和证据 URL 检查结果，来源只给日期时不虚构时刻。

该切片不实现全网采集、自动 Cron、LLM 抽取或付费 Provider。URL 检查受外部调用总开关与每批上限控制；关闭时安全降级为未确认线索。

## 官方工商身份核验与歧义重路由 V1

2026-07-16 根据 ADR-0008 实现 `OfficialIdentityProvider` 契约、本机官方结果 JSON 导入、政府/GSXT 域名限制、统一社会信用代码校验位、官方核验证据表与 RLS。工作台可以在默认 30 天有效期内展示与提及关联的候选；具有审核员+机构管理员角色的用户选定后，系统事务性更新公司身份、保留曾用名、解析实体提及、重建原事件/证据并复用 ADR-0007 路由。

本切片不抓取验证码、登录页或未公开接口，也不自动创建未匹配公司。政府官方查询结果仍可结构化导入；授权商业工商数据通过独立依据和 Provider 适配器接入。身份决定不做外部访问，所以原来源 URL 未检查的记录会重路由为未确认线索。

## 天眼查授权工商身份 Provider V1

2026-07-19 根据 ADR-0010，在不改变双通道公司访问、事件发布和共享晋升规则的前提下，增加 `licensed_business_data` 核验依据、`0013` 保守迁移、天眼查固定 Core 端点适配器、30 天私有响应缓存和本机受控导入命令。每家公司使用名称候选加信用代码精确查询，必要字段缺失或返回冲突时失败关闭；同代码但名称/地区冲突进入现有身份工作台，不静默改写主档。

本切片不加入同步搜索/详情、自动刷新或来源监测 Worker，不实现联系人、工商变更订阅、通用商业数据平台或自动发布。真实调用要求总开关和专用开关同时临时开启，保持付费、自动刷新和自动发布关闭；数据库只保存必要身份字段与响应哈希，完整响应留在 Git 忽略私有缓存。

## 全局公司身份索引与已核验别名共享 V1

2026-07-19 根据 ADR-0011 修正公司身份与基金归属边界：工商主体是平台全局主实体，基金和投资关系只做私有叠加，不拥有或锁定公司。全局目录公司在管理员显式完成身份确认并发生法定名称变更时，旧工商全称以无 owner 的已核验共享别名保存；旧全称、新全称和信用代码都命中同一 `company_id`。租户级公司、内部代号、品牌名和未核实名称保持私有。

本闸门的完成条件是：个人、另一机构和两个基金复用同一公司身份，且投资叠加、私有线索、文档和证据继续隔离；无外部调用、无自动发布、无批量历史回填。通过后才进入下一项运营验证，不在本里程碑扩展模糊搜索、品牌关系或事件共享。

## 双通道访问演进

2026-07-16 通过 ADR-0009 确认：近期采用个人查询/关注与现有基金模型共存的分步方案，长期目标为全局用户、个人订阅与 organization 机构授权并存的双通道架构。平台共享事实不等于匿名互联网公开；第一阶段仍要求登录和有效访问资格。事件事实、证据引用和原始文档分别判断作用域与许可。

### PR 1：双通道访问与数据作用域 ADR

已通过 PR #9 完成并合并；只修改 ADR、领域模型、数据库设计、API 契约、安全设计和实施顺序。

### 个人用户安全查询公司 MVP（合并原 PR 2、3、4 范围）

2026-07-17 为避免三次迁移和接口反复适配，在一个可回滚代码 PR 中完成原 PR 2、3、4 的依赖闭环：未确认线索、别名、原始文档、提及、事件、证据和快照作用域；owner 检查约束和保守回填；RLS；共享公司精确搜索/详情；基金私有叠加层；最小前端及双通道浏览器验证。

无法可靠确认历史所有者的记录保持 `system_restricted`；现有两家真实公司继续为租户级记录，没有批量晋升为平台共享。同步搜索无结果不创建公司、不调用外部 Provider。

本里程碑仍不做：模糊搜索自动绑定、个人 watchlist/备注、正式认证、subscription/entitlement、支付、organization 全量重构或外部 Provider。

### 后续验证与兼容

安全基线和最小个人路径完成后，下一步使用 8—10 家真实公司同时验证个人和机构两条路径。当前本地影子验证基金、两家公司、投资关系和授权继续保留；两家真实公司当前为租户级记录，只有逐条人工确认身份、许可和共享范围后才能晋升为平台共享公司。所有 Schema 变化继续使用新 Alembic 迁移，不修改历史迁移。

### 受控平台共享事实晋升闭环

2026-07-17 根据 10 家真实公司双路径影子验证结果，实现私有候选由 `platform_admin` 人工晋升为独立共享事实的最小闭环。原私有事件、导入批次、文档、owner 和研究上下文均不改变；共享事件通过全局指纹去重，展示证据只保存许可允许的来源定位、状态和短摘录，并以血缘字段指回私有证据。拒绝和撤回只追加审计，撤回后个人详情停止展示共享事实。

本里程碑不恢复自动发布，不调用外部 Provider，也不实现正文语义自动核验、官方工商自动抓取、正式认证、订阅、支付、watchlist 或复杂事件版本编辑。下一步优先补齐可授权的官方工商候选入口和身份核验资料，而不是扩大自动采集范围。
